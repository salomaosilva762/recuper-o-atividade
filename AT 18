

programa {
  funcao inicio() {
    real h, peso
    cadeia sexo
   escreva("qual suas altura ? ")
    leia(h)
escreva(" qual seu sexo ? ")
    leia(sexo)
    peso = 0.0
    se (sexo == "M") {
      peso = (72.7 * h) - 58
    }
    se (sexo == "F") {
      peso = (62.1 * h) - 44.7
    }
    escreva(peso)
  }
}
